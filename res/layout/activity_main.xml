<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

   <GridView
       android:id="@+id/gridView"
       android:layout_width="0dp"
       android:layout_height="0dp"
       android:layout_marginStart="10dp"
       android:layout_marginTop="50dp"
       android:layout_marginEnd="10dp"

       android:verticalSpacing="10dp"

       app:layout_constraintBottom_toBottomOf="parent"
       app:layout_constraintEnd_toEndOf="parent"
       app:layout_constraintStart_toStartOf="parent"
       app:layout_constraintTop_toBottomOf="@+id/myTextView" />

   <TextView
       android:id="@+id/myTextView"
       android:layout_width="0dp"
       android:layout_height="wrap_content"
       android:layout_marginTop="20dp"
       android:fontFamily="@font/mango"

       android:gravity="center"

       android:padding="10dp"
       android:text="Choose a shape and calculate the volume"
       android:textColor="#232D35"
       android:textSize="60sp"
       app:layout_constraintBottom_toTopOf="@+id/gridView"
       app:layout_constraintEnd_toEndOf="parent"
       app:layout_constraintStart_toStartOf="parent"
       app:layout_constraintTop_toTopOf="parent" />

   <!--GridView = tabel (daca sunt prea multe elemente se poate da scroll)
    1. Afișarea în grilă (randuri si coloane) EX: GALERIA
    2.meniu cu pictograme, în care fiecare celulă conține o imagine și un text sub ea
    3.Numărul de coloane se ajustează în funcție de dimensiunea ecranului.  android:numColumns="auto_fit"  android:stretchMode="columnWidth"
    4.GridView este proiectat în mod implicit pentru scrolare verticală, poți crea efectul de scrolare orizontală folosind un HorizontalScrollView

    -->

</androidx.constraintlayout.widget.ConstraintLayout>